[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'
sourceSets.main.java.srcDirs = ["src/", "build/antlr-src/"]
sourceSets.test.java.srcDirs = ["test/"]

def localProperties = project.file("../local.properties")

eclipse.project {
  name = appName + "-core"
}
dependencies {
}